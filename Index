<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>John Tekeridis</title>
  <style>
    :root {
      --bg: #0e0e0e;
      --text: #ffffff;
      --muted: #9c9c9c;
      --accent: #ff4f2e;
      --border: #262626;
      --font-main: -apple-system, BlinkMacSystemFont, "Inter", sans-serif;
      --font-title: "Inter", sans-serif;
    }

    body {
      background: var(--bg);
      color: var(--text);
      font-family: var(--font-main);
      margin: 0;
      padding: 40px 24px;
      line-height: 1.6;
    }

    .container {
      max-width: 880px;
      margin: 0 auto;
    }

    .badge {
      display: inline-block;
      padding: 6px 12px;
      border: 1px solid var(--border);
      border-radius: 8px;
      font-size: 12px;
      letter-spacing: 0.1em;
      color: var(--muted);
      text-transform: uppercase;
      margin-bottom: 24px;
    }

    h1 {
      font-family: var(--font-title);
      font-size: 56px;
      text-transform: uppercase;
      margin: 0 0 12px 0;
      letter-spacing: -1px;
    }

    p.subtitle {
      font-size: 20px;
      max-width: 600px;
      margin-bottom: 24px;
    }

    p.description {
      color: var(--muted);
      font-size: 16px;
      max-width: 650px;
      margin-bottom: 40px;
    }

    .links {
      display: flex;
      gap: 16px;
      flex-wrap: wrap;
    }

    .links a {
      color: var(--text);
      padding: 10px 18px;
      border: 1px solid var(--border);
      border-radius: 6px;
      text-decoration: none;
      font-size: 15px;
      transition: background 0.2s ease;
    }

    .links a:hover {
      background: var(--accent);
      border-color: var(--accent);
    }

    @media (max-width: 600px) {
      h1 {
        font-size: 40px;
      }
      p.subtitle {
        font-size: 18px;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    
    <div class="badge">CREATOR // ENTREPRENEUR</div>

    <h1>John Tekeridis</h1>

    <p class="subtitle">
      Demo headline text about your work and identity.
    </p>

    <p class="description">
      This is demo placeholder text. Replace this paragraph with a short, clean description about who you are, what you build, or what you specialize in. 
      Keep it simple and direct. You can write about business, creative work, technology, property, or anything else.
    </p>

    <div class="links">
      <a href="#">Email</a>
      <a href="#">Instagram</a>
      <a href="#">LinkedIn</a>
      <a href="#">Portfolio</a>
    </div>

  </div>
</body>
</html>
